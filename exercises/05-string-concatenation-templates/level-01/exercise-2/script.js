// TODO: Create a sentence by concatenating multiple strings
// Example: "Hello" + ", " + name + "! Welcome!"
const name = "dylan";
const greeting = "Hello";
const punctuation = "! Welcome!";

const sentence = greeting + ", " + name + punctuation;
console.log("sentence:", sentence);